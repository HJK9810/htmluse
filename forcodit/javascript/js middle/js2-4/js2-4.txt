js의 모듈화.
<script type="module" src="">
>> live server사용
>> 변수들은 공유 불가능.
>> 코드 수정, 저장시 자동 새로고침.

모듈 문법
export : 다른 파일에서도 사용 가능
>> 불러오는 과정 필수.
import {변수 or 함수} form '가져오는 파일';

* 모듈화시, 진입용인 js 하나만 불러와도 OK.

가져온 변수 이름 바꾸기?
import {변수 or 함수 as 바꾸는 이름} form '가져오는 파일';
