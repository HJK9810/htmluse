* 추상화 : 구체적 존재를 원하는 방향으로 간략화

* 캡슐화 : 특정 프로퍼티값을 정해진 메소드를 통해서만 접근하도록 하는것
set : setter메소드
set 메소드명(파라미터) {
  메소드내용
}
>> 유효성검사 등.
get : getter메소드
get 메소드명() {
  return 프로퍼티 값;
}
>> 파라미터는 딱히 작성X

* 상속 : 
class 자식클래스명 extends 부모클래스 {
  constructor(파라미터) {
    super(부모생성자 파라미터);
    this.파라미터 = 자식 파라미터;
  }
  겹치지 않는 부분들
}
>> 코드의 재사용성이 좋음