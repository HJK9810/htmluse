* return문
주 역할은 함수의 실행을 중단하는 역할!

옵셔널 파라미터
function 함수 (파라미터, 파라미터 = '') {
  내용.
}
>> 파라미터 = ''의 경우, 파라미터에 우선적으로 값을 배당. 
가장 뒷쪽에 선언을 하지 않는다면 생략시 파라미터가 앞으로 밀려버리는 문제 발생.

Scope : 범위, 영역
>> 글로벌(전역)변수 or 로컬(지역)변수

const : 상수
>> 변하지 않는 값, 변경할수 없는 값.
>> 주로 대문자, 단어와 단어간의 연결은 '단어_단어' 형식으로 사용.